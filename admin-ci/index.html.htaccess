RewriteEngine On

# Jangan redirect file & folder nyata
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d

# Arahkan semua request lain ke index.html (client-side routing)
RewriteRule ^(.*)$ index.html [L]
